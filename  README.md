## Design

The system is designed using the **Strategy** and **Factory** patterns.

- `Engine` is an interface (Strategy) that allows the car's engine to be swapped at runtime.
- `CarFactory` handles the creation and replacement of engines.
- The full UML class diagram is available in [`docs/Fatory.Car.drawio.png`](docs/Fatory.Car.drawio.png).

